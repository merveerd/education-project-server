const user = require("./user.controller");
const course = require("./course.controller");
const group = require("./group.controller");
const user_course = require("./user_course.controller");
module.exports = {
  user,
  course,
  user_course,
  group,
};
